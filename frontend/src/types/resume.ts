export interface ResumeResponse {
  id: string
  userId: string
  originalName: string
  rawText: string
  uploadAt: string
}

export interface ResumeSummaryResponse {
  id: string
  originalName: string
  uploadAt: string
}
